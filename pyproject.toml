[build-system]
requires = [
    "setuptools",
    "torch",
]
build-backend = "setuptools.build_meta"

[project]
name = "torchscience"
version = "0.0.1"
description = "Example of PyTorch C++ and CUDA extensions"
requires-python = ">=3.9"
dependencies = [
    "torch",
]
